#include <stdio.h>

/**
 * main - Prints alphabet to lowercase
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char ch;

	for(ch = 'a' ; ch <= 'z'; ch++);

	putchar(ch);

	putchar('\n');

	return (0);
}
